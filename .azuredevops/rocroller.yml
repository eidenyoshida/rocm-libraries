variables:
- group: common
- template: /.azuredevops/variables-global.yml@pipelines_repo

parameters:
- name: pipelinesRepoRef
  type: string
  default: refs/heads/develop
- name: triggerDownstreamJobs
  type: boolean
  default: false

resources:
  repositories:
  - repository: pipelines_repo
    type: github
    endpoint: ROCm
    name: ROCm/ROCm
    ref: ${{ parameters.pipelinesRepoRef }}

trigger:
  batch: true
  branches:
    include:
    - develop
    - release-staging/rocm-rel-7.*
  paths:
    include:
    - shared/rocroller
    exclude:
    - shared/rocroller/.github
    - shared/rocroller/.jenkins
    - shared/rocroller/docs
    - shared/rocroller/*.md
    - shared/rocroller/.*.y*ml

pr: none

stages:
- stage: rocroller
  jobs:
  - template: ${{ variables.CI_COMPONENT_PATH }}/rocRoller.yml@pipelines_repo
    parameters:
      sparseCheckoutDir: shared/rocroller
      triggerDownstreamJobs: ${{ parameters.triggerDownstreamJobs }}
- template: templates/report-summary-check-wrapper.yml
